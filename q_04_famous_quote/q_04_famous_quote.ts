function printQuoteAndAuthor(quote: string, author: string): void {
  console.log(`"${quote}" - ${author}`);
}

const quote = "The greatest glory in living lies not in never falling, but in rising every time we fall.";
const author = "Nelson Mandela";

printQuoteAndAuthor(quote, author);